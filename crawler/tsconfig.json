
{
	"extends": "../tsconfig.base.json",
	"compilerOptions": {
		"rootDir": "src",
		"outDir": "out",
		// Crawler is a Node CLI; stick with NodeNext module/resolution from base
		"types": ["node"],
		"noEmit": false,
		"noEmitOnError": false
	},
	"include": [
		"src/**/*"
	]
}
